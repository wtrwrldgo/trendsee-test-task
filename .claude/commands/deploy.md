# /deploy — Deploy Frontend to Vercel

Deploy the frontend to Vercel production.

## Steps
1. Run `cd frontend && vercel --prod --yes`
2. Report the production URL
3. Ask if user wants to commit and push changes to GitHub too
